{{!< default}}

<div class="content-area">
<main class="site-main">

    <div class="post-feed">
        {{#get "posts" filter="tag:portfolio"}}
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        {{/get}}
    </div>

</main>
</div>
